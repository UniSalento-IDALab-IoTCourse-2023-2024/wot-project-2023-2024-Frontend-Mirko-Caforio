import analytics from "./analytics";
import machine from "./machine";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  supervisorItems: [analytics, machine],
  adminItems: [analytics, machine]
};

export default menuItems;
